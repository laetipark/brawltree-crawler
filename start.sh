# npm 모듈 설치
$(which npm) install

# NestJS 앱 build
$(which npm) run build

# 데이터베이스 테이블 생성
$(which npm) run migration:run

# Seed 데이터 삽입
$(which npm) run seed:run

# NestJS 앱 실행
$(which npm) run start:prod